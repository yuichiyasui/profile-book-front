{
  "extends": ["stylelint-config-standard"],
  "ignoreFiles": ["**/node_modules/**"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      { "ignoreAtRules": ["include", "mixin", "each"] }
    ]
  }
}
